<h1>Existing games</h1>
<p>Looks like you're already running this game.  Pick from the list below or choose "New Game"</p>

<ul class="list-unstyled">
	{{#each existing}}
	<li>
		<strong>{{game.name}}</strong> {{#each players}}|{{/each}} 
		<a href="/activity/run/{{_id}}" class="btn btn-success"><span class="glyphicon glyphicon-send"></span> Host</a>
		<a href="/activity/delete/{{_id}}" class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Delete</a>
		<a href="/activity/join/{{_id}}" class="btn btn-primary"><span class="glyphicon glyphicon-play"></span> Play</a>
	</li>
	{{/each}}
	<li>No, none of these <a href="/activity/from/{{game._id}}?new=1" class="btn btn-success">Create New</a></li>
</ul>

